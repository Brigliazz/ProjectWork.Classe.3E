<Window x:Class="BlaisePascal.ProjectWork3E.Wpf.ConfermaCapienzaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Capienza Superata" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" SizeToContent="Height">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icona Warning -->
        <TextBlock Text="⚠" FontSize="42" Foreground="#FFA500" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,15,0"/>

        <!-- Testo Parametrico -->
        <TextBlock x:Name="MessageText" Grid.Column="1" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="14"/>

        <!-- Pulsanti -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Procedi" Width="80" Margin="0,0,10,0" Padding="5" Click="BtnProcedi_Click" IsDefault="True"/>
            <Button Content="Annulla" Width="80" Padding="5" Click="BtnAnnulla_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
